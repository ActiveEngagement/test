name: Example

on:
  push:
    branches: [ master ]

jobs:
  full-build:

    runs-on: ubuntu-latest
    
    steps:
      - uses: ActiveEngagement/deployer-action@master
        with:
          artifacts: |
            {
              "public/build": "build"
            }
